# ===========================================
# RAILWAY DEPLOYMENT - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to set up your Railway environment variables
# DO NOT commit actual values to version control

# ===========================================
# BACKEND ENVIRONMENT VARIABLES
# ===========================================

# Django Settings - OBRIGATÓRIO CONFIGURAR NO RAILWAY
SECRET_KEY=your-super-secret-key-here-min-50-chars-long-random-string
DEBUG=False
DJANGO_SETTINGS_MODULE=core.settings.production

# Domain Configuration
ALLOWED_HOSTS=your-backend-domain.railway.app,your-frontend-domain.railway.app
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.railway.app
FRONTEND_URL=https://your-frontend-domain.railway.app

# Database (Railway PostgreSQL)
# Railway automatically provides DATABASE_URL
# You can also set individual values:
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=your-db-host.railway.app
DB_PORT=5432

# Redis (Railway Redis)
# Railway automatically provides REDIS_URL
REDIS_URL=redis://default:password@host:port

# Email Configuration (Example with SendGrid)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your-sendgrid-api-key
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# AI Integration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Open Banking / Pluggy
PLUGGY_CLIENT_ID=your-pluggy-client-id
PLUGGY_CLIENT_SECRET=your-pluggy-client-secret
PLUGGY_USE_SANDBOX=true
PLUGGY_BASE_URL=https://api.pluggy.ai

# Payment Gateways
DEFAULT_PAYMENT_GATEWAY=stripe

# Stripe
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Payment Gateway Configuration (Stripe only)
# Note: MercadoPago integration has been removed

# AWS S3 (for file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name
AWS_S3_REGION_NAME=us-east-1

# Monitoring (Optional)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Business Configuration
TRIAL_PERIOD_DAYS=14
DEFAULT_CURRENCY=BRL

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================

# API Configuration
NEXT_PUBLIC_API_URL=https://your-backend-domain.railway.app

# App Configuration
NEXT_PUBLIC_APP_NAME=CaixaHub
NEXT_PUBLIC_APP_URL=https://your-frontend-domain.railway.app

# Payment Gateway Public Keys
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key
# NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY=removed (Stripe only)

# Analytics (Optional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ===========================================
# RAILWAY SPECIFIC NOTES
# ===========================================
# 
# 1. Railway automatically provides:
#    - PORT (for both frontend and backend)
#    - DATABASE_URL (if PostgreSQL addon is added)
#    - REDIS_URL (if Redis addon is added)
#
# 2. Backend Variables to set in Railway:
#    - Copy all "BACKEND ENVIRONMENT VARIABLES" above
#    - Railway will provide DATABASE_URL and REDIS_URL automatically
#
# 3. Frontend Variables to set in Railway:
#    - Copy all "FRONTEND ENVIRONMENT VARIABLES" above
#    - Make sure NEXT_PUBLIC_API_URL points to your backend Railway URL
#
# 4. Don't forget to:
#    - Add PostgreSQL addon in Railway
#    - Add Redis addon in Railway
#    - Update ALLOWED_HOSTS with your Railway domains
#    - Update CORS_ALLOWED_ORIGINS with your frontend domain
#
# ===========================================