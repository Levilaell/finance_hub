{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python -c \"from django.db import connection; cursor = connection.cursor(); cursor.execute('ALTER DATABASE railway REFRESH COLLATION VERSION;'); print('✅ Collation refreshed')\" && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 4"
  }
}